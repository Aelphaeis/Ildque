language: java
install: true
jdk:
  - oraclejdk8

deploy:
  skip_cleanup: true
  provider: script
  script: cp .travis.settings.xml $HOME/.m2/settings.xml && mvn tomcat7:redeploy
  on:
    branch: master